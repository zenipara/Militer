/**
 * Feature: Announcements (Pengumuman)
 *
 * Barrel export untuk fitur pengumuman.
 *
 * Penggunaan:
 *   import { useAnnouncements } from '@/features/announcements';
 */
export { useAnnouncements } from '@/hooks/useAnnouncements';
export type { Announcement } from '@/types';
