/**
 * Feature: Messages (Pesan)
 *
 * Barrel export untuk fitur pesan internal.
 *
 * Penggunaan:
 *   import { useMessages } from '@/features/messages';
 */
export { useMessages } from '@/hooks/useMessages';
export type { Message } from '@/types';
